# R1 static and general regression

**Verdict:** `PASS`

The canonical Paper I static runner passed 95/95 checks against
`preprint_draft_v1.3`. It verified artifact presence and hashes, formal source
and package manifests, recorded build/axiom evidence, all earlier manuscript
corrections, bilingual structure, figures, PDF text/fonts/pages, formal
identifiers, self-containment and absence of manuscript compiler residue.

The runner was executed from its canonical `preprints/_audit_tools` location;
an attempted execution of a nested copy was discarded because that copy derived
the wrong repository root before reading the target.

