# Paper I formalization

`lean_v1.2_freeze/` is the publishable frozen source snapshot used by Paper I
v1.3. It contains pinned dependencies, recorded local build logs, theorem-level
axiom outputs, manifests and `PAPER_I_lean_v1.2_freeze.zip`.

The exact archive passed external clean-room reproduction. The release
promotion did not modify any Lean source, build log, axiom output or archive
byte.
