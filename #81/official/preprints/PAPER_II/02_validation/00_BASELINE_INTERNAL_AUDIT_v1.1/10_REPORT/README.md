# Report workspace

The Markdown and TeX files in this directory are blank audit templates. Do not
rename either to `FINAL_AUDIT_REPORT` or generate a certificate until the audit
has been executed, reviewed and admitted. The completed Markdown file is the
source of truth; any PDF must be rendered and visually checked.
