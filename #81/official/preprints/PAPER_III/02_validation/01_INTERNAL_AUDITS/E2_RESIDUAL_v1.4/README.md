# Paper III v1.4 — internal E2 residual

This directory records an author-side rederivation of the mathematical items
left `NOT ATTEMPTED` or `PARTIAL` by the external v1.3 audit.  It is not an
external audit and does not replace independent review.

The target is the unchanged English mathematical source inherited from v1.3.
The residual covers `K-EPS`, `K-CORRIDOR`, `K-SPARSE`, and `K-GLOBAL`, plus the
previously partial hypothesis chain in Lemma 7.1.  `K-COVER` and Sections 4 and
9 are regression interfaces because the external auditor already rederived
them.

The exact-arithmetic program is a falsifier and algebraic certificate.  The
universal arguments are in `10_DERIVATIONS`; finite searches are never used as
proofs of universally quantified statements.

