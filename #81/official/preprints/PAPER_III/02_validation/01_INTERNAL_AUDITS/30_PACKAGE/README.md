# Sealed internal-audit package

`INTERNAL_AUDIT_PACKAGE.zip` contains the final `00_CONTROL`, `10_REPORT`, and `20_EVIDENCE` trees together with `INTERNAL_AUDIT_SHA256.txt`.

The package records an internal, author-side, non-independent audit. It does not replace the requested external adversarial audit.
