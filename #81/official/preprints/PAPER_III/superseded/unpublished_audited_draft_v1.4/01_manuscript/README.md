# Paper III v1.4 manuscript artifacts

This directory contains one synchronized publication target in English and Spanish:

- Markdown semantic source;
- LaTeX generated from that Markdown using the established Paper III template;
- PDF compiled from the final LaTeX with LuaLaTeX.

The six artifacts pass the 51-check consistency suite, including aligned heading depth,
section-block counts, 205 display-math blocks, equation tags, citation sequence, Lean
identifiers, protected provenance strings and translation-loss sentinels. No duplicated
long paragraph occurs in either Markdown source. All 93 PDF pages were rendered and
inspected. Exact hashes are in `PAPER_III_preprint_draft_v1.4_SHA256.txt`.

The prior v1.3 target is preserved only in its separate version directory; no v1.3
publication artifact is duplicated here.
