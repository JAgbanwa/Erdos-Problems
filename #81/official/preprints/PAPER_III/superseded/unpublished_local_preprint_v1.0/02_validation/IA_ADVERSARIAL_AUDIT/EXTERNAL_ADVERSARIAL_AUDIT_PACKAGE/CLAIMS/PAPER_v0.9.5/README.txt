Paper III style-matched bilingual review package v0.9.5

The LaTeX files use the Paper II preprint preamble, title block, metadata block,
link styling, paragraph spacing, section hierarchy, and figure environments.

The PDFs were compiled directly from the supplied LaTeX files with LuaLaTeX,
compiled twice, and rendered for visual inspection.

Lean status remains provisional.
